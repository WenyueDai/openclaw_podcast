# Copy this file to .env and fill in your values.
# Then load it before running: export $(cat .env | xargs)
# Or use: source .env

# ── Required ───────────────────────────────────────────────────────────────────
# OpenRouter API key — free account at https://openrouter.ai
# After signing up, go to: https://openrouter.ai/settings/keys → Create key
# The free-tier models in config.yaml cost nothing.
OPENROUTER_API_KEY=sk-or-v1-your-key-here

# ── Optional overrides ─────────────────────────────────────────────────────────
# Override run date (YYYY-MM-DD). Leave unset to use today.
# RUN_DATE=2026-02-28

# How many hours back to look for new articles (default: 48)
# LOOKBACK_HOURS=48

# Set to "true" to skip real LLM calls and use cached/stub data
# DEBUG=false

# Rebuild podcast script from already-fetched items (skips RSS/PubMed fetch)
# REGEN_FROM_CACHE=false

# Tip: to reduce parallel LLM calls on the free tier, set fetch_workers: 4
# in config.yaml under the top-level keys.
