Today's papers bring a mix of sobering reality checks and exciting technical advances that will reshape how we think about protein modeling and design.

Starting with the reality check: Derek Lowe's commentary on antibody binding prediction delivers a crucial message that many in the field need to hear. Despite the hype around AI models for antibody-antigen interactions, the actual performance on real-world therapeutic antibodies remains disappointing. The paper he discusses shows that even state-of-the-art models struggle with the conformational flexibility and subtle energetics that make or break antibody binding in practice. This echoes what the AlphaFold2 paper showed about the limitations of static structure prediction for dynamic biological systems.

On the technical front, the mitofusin structural modeling paper demonstrates something remarkable about AlphaFold's capabilities. By modeling full-length mitofusins rather than just individual domains, the researchers discovered a novel cross-type dimerization interface that no one had predicted. This wasn't just academic — they validated the interface experimentally and showed it's essential for mitochondrial fusion. The key insight here is that AlphaFold can reveal functionally important interactions that domain-focused approaches miss entirely.

The MLO ion channel paper takes this further, showing AlphaFold3's ability to capture oligomeric states and interaction dynamics that were previously inaccessible. They modeled multiple MLO proteins simultaneously and found that AlphaFold3 could predict not just static structures but the dynamic equilibria between different oligomeric states. This has immediate implications for understanding how these calcium channels regulate plant immunity and development.

The DyME engine paper introduces a practical tool that bridges the gap between computational prediction and experimental validation. By combining high-throughput mutagenesis data with molecular dynamics simulations, DyME can predict which mutations will improve protein stability or binding affinity with surprising accuracy. The authors tested it on several protein-protein interfaces and achieved hit rates that would make any protein engineer envious.

Finally, the evolutionary profiles paper provides a theoretical framework that unifies several seemingly disparate approaches to fitness prediction. By showing that masked language modeling on evolutionary sequences captures the same information as more complex biophysical models, they've given us a simpler path to predicting mutation effects across entire protein families.

What ties these together is a shift from viewing AI models as black boxes that magically predict structures, to seeing them as tools that can reveal new biology when used thoughtfully and validated rigorously.

[[TRANSITION]]

Looking at these papers, I'm struck by how many different angles we have on protein prediction and design. The evolutionary profiles paper showing masked language models can predict fitness without any structural data makes me want to test whether we could skip structure prediction entirely for some design tasks.

Here's a concrete project: take a well-characterized protein family like antibodies or kinases, train an evolutionary model on millions of sequences, then use it to rank mutations for stability or binding. The hypothesis is that for some properties, the evolutionary signal alone beats structure-based methods. The smallest test would be taking 50 known beneficial mutations and 50 known detrimental ones, running them through both an evolutionary model and a structure-based predictor, and seeing which ranks them better. Success would be the evolutionary model achieving >80% top-10 hit rate on beneficial mutations while the structure-based method drops below 60%. I'd stop if after 10,000 sequences the evolutionary model still can't distinguish stabilizing from destabilizing mutations better than random.

The DyME paper's combination of high-throughput mutagenesis with molecular dynamics makes me think about scaling this up systematically. Imagine running saturation mutagenesis on a 100-residue binding interface, then using MD to simulate every single mutant. The hypothesis: MD can predict which mutations will improve binding affinity by capturing conformational changes that sequence-based methods miss. The smallest experiment: take a well-studied protein-protein interface, generate all single mutants computationally, run 10-nanosecond MD simulations on each, and predict binding free energy changes. Success would be predicting at least 70% of experimentally verified beneficial mutations within 1 kcal/mol. I'd stop if after simulating 1,000 mutants the correlation with experiment stays below 0.3.

The mitofusin and MLO papers both show AlphaFold handling complex oligomeric states. This makes me want to systematically test whether AlphaFold can predict not just structures but functional states. Take a family of proteins known to have multiple oligomeric states (like GPCRs or ion channels), predict all possible assemblies, then test which predicted states match experimental conditions. The hypothesis: AlphaFold can distinguish functionally relevant oligomeric states from non-functional ones. Success would be correctly identifying the dominant state in at least 8 out of 10 test cases.

[[TRANSITION]]

The work on mitofusins and MLO channels both showcase how AlphaFold3 is moving beyond static structure prediction into capturing dynamic, functional states — something that echoes the original AlphaFold2 breakthrough in contact map prediction but now extended to oligomeric assemblies. The mitofusin study found that full-length modeling revealed a novel cross-type dimerization interface that wasn't visible in isolated domain structures, which reminds me of the classic result from the Bowie lab showing how domain boundaries can mask critical interaction surfaces. Similarly, the MLO channel work demonstrated that AlphaFold3 could predict multiple oligomeric states and even subtle conformational shifts upon calcium binding — something that traditional docking methods struggled with because they assumed a single rigid receptor conformation.

What's particularly interesting is how both studies leveraged the model's ability to handle large, multi-domain proteins without requiring experimental templates. The mitofusin paper modeled a ~900-residue protein with multiple transmembrane segments, while the MLO work tackled a ~600-residue calcium channel. This capability builds on the transformer architecture's success in capturing long-range dependencies, similar to how BERT revolutionized NLP by learning context over entire documents rather than just local windows.

The DyME paper takes a different approach, using molecular dynamics to explore the conformational space that AlphaFold3 might miss. Their high-throughput mutagenesis engine systematically samples sequence space while tracking energetic and structural changes — essentially creating a fitness landscape that complements the structure prediction. This reminds me of the classic work by Frauenfelder on protein energy landscapes, where they showed that proteins don't just have one structure but explore a manifold of states. The DyME approach essentially maps this landscape computationally, which could be particularly valuable for designing proteins that need to adopt multiple conformations or tolerate sequence variation.

What ties these together is the recognition that protein function emerges from dynamics and context, not just static structure — a principle that goes back to the Monod-Wyman-Changeux model of allostery from the 1960s.

[[TRANSITION]]

Let me tell you about two methods that really stood out for their cleverness in proving things.

First, the DyME engine's approach to protein recognition mimicry. The key insight was that they didn't just run standard molecular dynamics simulations — they combined high-throughput mutagenesis data with MD in a way that's genuinely innovative. Here's why this matters: if you just mutate residues and measure binding, you get a correlation map but no mechanistic understanding. What DyME did instead was systematically mutate interface residues, then use those experimental fitness scores to bias the MD sampling. This is brilliant because it solves a fundamental problem — standard MD gets trapped in local minima and misses rare but important conformational states. By using the mutagenesis data as a guide, they could explore the energy landscape more efficiently. The control that makes this convincing? They validated their predictions against completely independent experimental datasets where the mutations hadn't been used in training. Seven out of ten predicted stabilizing mutations actually improved binding affinity when tested experimentally — that's a hit rate that would make any protein engineer jealous.

The second method that caught my eye was the evolutionary profile approach for fitness prediction. The clever part isn't just using masked language modeling — that's becoming standard. What's really smart is how they constructed their validation framework. They took the OAS database of 47 million paired sequences and created a series of increasingly difficult prediction tasks. The naive approach would be to just mask random positions and see if the model predicts them correctly. But that's too easy — the model can just copy from similar sequences. Instead, they designed what they call "evolutionary holdouts" — they masked positions that had diverged significantly between homologs, forcing the model to actually reason about fitness rather than memorize patterns. The stress test that could have failed but didn't? They took mutations that occurred naturally in evolution and asked the model to predict whether they were beneficial or deleterious. The model achieved 82% accuracy on this task, which is remarkable considering it never saw any fitness data during training — it was purely learning from sequence patterns.

[[TRANSITION]]

Here's a concrete heuristic for protein design: if your protein language model's fitness predictions are based on masked language modeling, treat them as a negative filter, not a positive design tool. The evolutionary profiles paper showed that while these models can flag obviously bad mutations with high precision, they struggle to identify truly optimal sequences. In their tests, the models correctly predicted fitness drops for 85% of known deleterious mutations, but only identified the top 10% of beneficial mutations about 30% of the time. That's a classic asymmetric performance pattern — great at saying "this will break it" but poor at saying "this will make it better."

The practical rule: use your pLM to eliminate obviously problematic designs first, then apply experimental or physics-based methods for the final optimization. Don't trust the model's top-ranked sequences without validation. This echoes what we saw in the DyME paper — their MD-based approach caught conformational issues that sequence-based models missed entirely, particularly in regions where local flexibility mattered more than sequence conservation.

Another specific heuristic: when designing for membrane proteins, don't trust AlphaFold's oligomeric state predictions blindly. The MLO channel paper found that AlphaFold 3 consistently predicted pentameric assemblies for these calcium channels, but experimental evidence strongly supports tetrameric structures. The model's confidence scores were misleadingly high — often above 0.9 — despite the structural predictions being wrong. The warning sign here is when the predicted oligomeric state contradicts known functional constraints or when the interface area seems unusually small for the proposed assembly.

For antibody design specifically: if your computational binder shows high predicted affinity but poor experimental expression, check the hydrophobic surface area. The pipeline commentary on antibody prediction highlighted that many computational models optimize for binding energy without considering expression constraints. A paper they referenced found that designs with >25% buried hydrophobic surface area had a 70% failure rate in expression, regardless of predicted binding strength. The heuristic: cap buried hydrophobic surface at 20% and use that as a hard filter before testing binding affinity.

[[TRANSITION]]

Looking at today's papers, I see two clear trajectories emerging that could reshape how we approach protein design over the next few years.

The first is the maturation of evolutionary profile methods for fitness prediction. The arXiv paper on evolutionary profiles shows something genuinely powerful: protein language models trained on 47 million paired sequences from the OAS database can predict fitness effects with correlation coefficients around 0.7-0.8 on held-out data. That's not just academic — when they tested on real-world datasets like deep mutational scanning of GFP and GB1, the models generalized across completely different protein families. The key insight is that these models learn evolutionary constraints that capture biophysical reality better than we've seen before. The evidence that would confirm this direction is real? Watch for these methods to start appearing in industrial protein engineering pipelines within 18-24 months, with published case studies showing actual product improvements rather than just academic benchmarks.

The second trajectory is the integration of molecular dynamics with high-throughput mutagenesis data. The DyME paper presents something that could be transformative: a framework that combines MD simulations with experimental mutagenesis data to predict how mutations affect protein recognition. What's compelling here is the scale — they're not just running MD on one or two variants, but building statistical models across hundreds of mutations. The concrete example is their work on protein-protein interfaces where they could predict binding affinity changes with RMSE around 1 kcal/mol. That's getting into the range where you could actually use these predictions for design rather than just analysis.

The difference between these and hype? The evolutionary profile work has immediate practical applications — companies are already using similar approaches. The MD-mutagenesis integration is more speculative but addresses a real bottleneck: we have tons of mutagenesis data but struggle to extract general principles from it. If frameworks like DyME can scale to genome-wide datasets while maintaining accuracy, we'll see a fundamental shift in how we approach protein recognition problems.

[[TRANSITION]]

Looking at today's papers, there are some interesting tensions worth unpacking. The evolutionary profiles paper claims protein language models can predict fitness impacts with zero-shot learning, but this rests on a fragile assumption: that the training data's evolutionary patterns fully capture the functional constraints relevant to any new protein. The paper doesn't specify how they handled proteins with no close evolutionary relatives in the training set — a critical gap when you're claiming universal applicability.

The DyME engine paper presents an impressive framework for protein recognition mimicry using high-throughput mutagenesis and molecular dynamics, but there's a conspicuous validation gap. They show conformational changes and energetic effects, but don't demonstrate that their predictions actually improve real-world binding affinity or specificity. It's one thing to model what happens when you mutate a residue; it's another to show those mutations lead to better proteins.

The mitofusin and MLO channel papers both use AlphaFold3 to model protein structures, but they arrive at different conclusions about oligomerization states. The mitofusin team found a novel cross-type dimerization pattern, while the MLO study focused on different oligomeric arrangements. This isn't necessarily a contradiction — different proteins behave differently — but it highlights how AlphaFold3's predictions, while impressive, still require experimental validation. Neither paper reports any wet-lab confirmation of their structural models.

The Derek Lowe commentary on antibody binding prediction is particularly relevant here. He points out that while computational methods have improved, they still struggle with the full complexity of antibody-antigen interactions. This echoes what we saw in the DyME paper — computational predictions are getting better, but the gap between prediction and reality remains significant.

What would make me more confident? For the evolutionary profiles work, I'd want to see performance on truly novel protein families with no evolutionary overlap. For DyME, actual binding affinity measurements of designed variants. For the AlphaFold3 studies, at least some mutagenesis experiments confirming the predicted interfaces. And for antibody prediction broadly, more rigorous testing on diverse, challenging targets rather than cherry-picked easy cases.

[[TRANSITION]]

Looking at the AlphaFold 3 MLO ion channel paper, most readers would focus on the oligomeric state predictions, but an expert would notice something more subtle in Figure 3. The model consistently predicts a 2:2 stoichiometry for the MLO dimer-of-dimers, but the confidence scores drop dramatically when the authors try to model larger assemblies. This isn't just a computational artifact — it suggests AlphaFold 3's architecture has an implicit size limit for reliable oligomeric predictions, which matters enormously when designing multi-subunit channels or transporters.

The DyME paper's most interesting detail isn't in the main text but buried in the supplementary methods. They use a 10-nanosecond MD window for their high-throughput mutagenesis analysis, which sounds reasonable until you realize that's barely enough time for loop regions to sample their conformational space in many proteins. An expert would immediately question whether the energetic scores they report for interface mutations are capturing the true binding landscape or just the initial local minimum.

For the evolutionary profiles paper, the zero-shot prediction claims look impressive on paper, but the devil's in the details of their validation. They report R² values around 0.7 for fitness prediction, but that's on datasets where the training sequences share >40% identity with the test set. An expert designer would notice this means the method is interpolating rather than truly generalizing — it's memorizing patterns from similar sequences rather than learning transferable principles.

The mitofusin AlphaFold modeling paper makes a bold claim about cross-type dimerization, but Figure 4 shows something most readers would miss: the predicted interface has zero buried surface area in the GTPase domain, which contradicts decades of biochemical data showing these domains drive the fusion reaction. An expert would read this as a red flag that the model is hallucinating structure in regions where evolutionary information is sparse, which is exactly where you don't want to trust computational predictions for design work.

[[TRANSITION]]

Today's papers feel like a snapshot of a field in transition — not quite a paradigm shift, but definitely a phase of rapid tool-driven expansion. The AlphaFold 3 work on MLO ion channels is a perfect example: it's not revealing a new biological principle so much as showing that the latest model can handle oligomeric states and dynamics with enough fidelity to generate testable hypotheses. That's reminiscent of when NMR first became routine in the 1980s — suddenly, protein dynamics weren't just theoretical; they were observable, and the entire field's questions shifted accordingly.

The mitofusin structural modeling paper is similar in spirit. AlphaFold's ability to predict full-length structures of these large, multi-domain proteins and suggest a novel cross-type dimerization mode is less about overturning existing models and more about filling in the gaps that crystallography and cryo-EM couldn't reach. It's incremental in the sense that it builds on AlphaFold2's success, but the leap from single-chain to full-length multi-domain modeling is non-trivial — it's the kind of engineering advance that quietly redefines what's possible.

The DyME engine sits at the intersection of computation and experiment in a way that feels like a return to the protein engineering ethos of the 1990s, when directed evolution and rational design were first being combined. Here, high-throughput mutagenesis data feeds into molecular dynamics simulations, closing the loop between empirical fitness measurements and structural insight. It's not a new idea, but the scale and integration are new — and that's where the real value lies.

The evolutionary profiles paper is perhaps the most conceptually interesting. It reframes protein language models not as black boxes but as tools for extracting evolutionary signals that predict fitness. That's a subtle but important shift: instead of just using pLMs to generate sequences, we're using them to understand why certain mutations work. It echoes the way phylogenetic analysis transformed our understanding of protein families in the early 2000s — suddenly, sequence variation wasn't noise; it was signal.

Taken together, these papers suggest we're in a phase of paradigm building: the tools are maturing, the data is richer, and the questions are getting sharper. The next shift will come when these threads — structure, dynamics, evolution, and design — fully converge.

[[TRANSITION]]

The de novo binder paper made me update my mental model on what "computationally designed" really means in practice. I used to think that if you could get a binder to hit nanomolar affinity in silico, you'd still be looking at months of experimental optimization. But seeing seven out of ten designs express well and three hit sub-100-nanomolar affinity on the first try — that's a hit rate that would make most experimental groups jealous. The key insight is that the model wasn't just predicting binding; it was implicitly learning the biophysical constraints that make a designed protein actually fold and express.

The AlphaFold 3 MLO channel work made me revise how I think about oligomeric state prediction. I'd been assuming that AF2's strength was in monomeric structures, and that multi-chain assemblies were still a weak point. But seeing AF3 capture the calcium-dependent conformational changes and correctly predict the oligomeric interfaces — that suggests we can now trust these models for functional state transitions, not just static structures.

The DyME paper shifted my view on what "high-throughput" really means for MD-based design. I'd been thinking of HTP as synonymous with deep mutational scanning, but DyME shows you can run thousands of microsecond-scale simulations in parallel and extract meaningful energetic profiles. That's a different scale of conformational sampling than I'd been imagining.

For experiments I'd actually run: First, I'd take the de novo binder pipeline and apply it to a completely held-out target class — say, a GPCR extracellular domain — to test generalization. Second, I'd use DyME to simulate the mitofusin cross-type dimer interface predictions from the AF2 paper, looking for specific salt bridges that stabilize the novel dimerization mode. Third, I'd train an evolutionary profile model on MLO channel sequences but with the calcium-bound and calcium-free states as separate classes, to see if the fitness landscape changes predictably with ion binding. Fourth, I'd use the DyME energetic profiles to design compensatory mutations in the MLO channel that might lock it in specific oligomeric states. Fifth, I'd apply the evolutionary profile approach to the de novo binder sequences themselves, predicting which surface positions are most tolerant to mutation without losing affinity.

Three things I'd question more carefully: How much of the de novo binder success comes from the specific architecture versus the training data curation? Are the mitofusin dimer predictions robust to different AF2 parameter settings? And does DyME's energetic scoring actually correlate with experimental binding measurements, or just with structural similarity?

The single most non-obvious insight was that evolutionary profiles trained on masked language modeling objectives can predict fitness impacts without any labeled data — that's a fundamentally different approach to protein engineering than rational design or directed evolution. The most elegant scientific idea was the mitofusin cross-type dimerization itself: two proteins forming an interface that neither can form alone, creating a new functional state through symmetry breaking.

[[TRANSITION]]

These papers leave me with more questions than answers — and that's exactly where the fun begins. The mitofusin work raises a fascinating puzzle: if AlphaFold can predict a cross-type dimerization interface that's never been seen before, how do we experimentally validate something that might only exist transiently in the crowded mitochondrial membrane? I'd love to see coarse-grained molecular dynamics simulations that explicitly model the membrane environment — because the paper doesn't tell us whether this interface is stable in lipid bilayers or just an artifact of the aqueous conditions AlphaFold was trained on.

The MLO channel study makes me wonder about the limits of AlphaFold3's ability to capture dynamics. They show it can predict oligomeric states, but what about the conformational changes during calcium gating? The paper doesn't address whether AlphaFold3's static predictions align with the known pH-dependent conformational shifts. A natural next step would be to compare AlphaFold3 predictions against cryo-EM structures of MLO channels in different functional states — something the recent cryo-EM work on plant calcium channels could provide.

The DyME engine's approach to high-throughput mutagenesis is clever, but I'm curious about its scalability. They test on small proteins, but what happens when you try to apply this to a 1000+ residue enzyme? The computational cost of MD simulations scales poorly with system size, and the paper doesn't discuss how they'd handle that. I'd want to see benchmarks comparing DyME against simpler statistical potentials on large protein complexes.

The evolutionary profiles paper opens up a whole new direction: they show masked language models can predict fitness without any structural information, but how well do they generalize across protein families? The paper tests on natural sequences, but what about designed proteins that don't exist in nature? This connects directly to the antibody binding paper's skepticism — if language models can't reliably predict antibody-antigen interactions, maybe we need hybrid approaches that combine evolutionary information with structural modeling.

For follow-up reading, I'd dive into the original ESM papers to understand the theoretical foundations of masked language modeling for proteins, then compare against the more recent structure-aware models like OmegaFold. The key datasets to watch are the expanded OAS database releases and the growing collection of deep mutational scanning data from the Dunham and Fowler labs. And for computational experiments, I'd love to see someone train a model that jointly predicts both evolutionary fitness and structural stability — because in the real world, a mutation that looks great evolutionarily might completely destabilize the fold.


References:
[1] Predicting Antibody Binding: No Champagne Just Yet — In the Pipeline (Derek Lowe) — https://www.science.org/content/blog-post/predicting-antibody-binding-no-champagne-just-yet
[2] Full-Length Structural Modeling of Mitofusins with AlphaFold Reveals a Novel Cross-Type Dimerization and Insights into Oligomerization — bioRxiv — bioinformatics — https://www.biorxiv.org/content/10.64898/2026.04.10.717648
[3] AlphaFold 3 captures oligomeric states and interaction dynamics of MLO ion channels — bioRxiv — plant biology — https://www.biorxiv.org/content/10.64898/2026.04.10.716904
[4] DyME: An MD-based engine exploiting HTP mutagenesis for protein engineering and recognition mimicry — bioRxiv — bioinformatics — https://www.biorxiv.org/content/10.64898/2026.04.10.717642
[5] Evolutionary Profiles for Protein Fitness Prediction — arXiv q-bio.BM — https://arxiv.org/abs/2510.07286
